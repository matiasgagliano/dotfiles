#
# Environment
#

# Set default editor
export EDITOR='/usr/bin/vim'
export VISUAL="$EDITOR"

# Pager (overwrite default $LESS to make `less` clear the screen on exit)
export PAGER='less'
export LESS='-R'
